name: erp_homepage
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  table_calendar: ^3.1.1
  get: ^4.6.6
  animated_custom_dropdown: ^3.0.0
  google_fonts: ^6.2.1
  file_picker: ^8.0.3
  easy_stepper: ^0.8.5
  intl_phone_field: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - resources/jsonformatter.txt

